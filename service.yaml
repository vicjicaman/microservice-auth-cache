apiVersion: v1
kind: Service
metadata:
  name: '${SERVICE_CACHE@microservice-auth-config}'
  namespace: '${NAMESPACE@microservice-config}'
  labels:
    moduleid: microservice-auth-data
    version: 1.70.2-auth-base-cache-data-prod
spec:
  clusterIP: None
  ports:
    -
      port: '${INTERNAL_PORT_CACHE@microservice-auth-config}'
      targetPort: '${INTERNAL_PORT_CACHE@microservice-auth-config}'
  selector:
    app: '${STATEFUL_CACHE@microservice-auth-config}'
